
<%= form_for(@trial) do |f| %>
  <% if @trial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trial.errors.count, "error") %> prohibited this trial from being saved:</h2>

      <ul>
      <% @trial.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Customer name" %><br />
    <%= f.text_field :customername %>
  </div>
  <div class="field">
    <%= f.label "Customer email"  %><br />
    <%= f.text_field :customeremail %>
  </div>
  <div class="field">
    <%= f.hidden_field :instancename %>
  </div>
  <div class="field">
    <%= f.hidden_field :instanceid %>
  </div>
  <div class="field">
    <%= f.hidden_field :imageid %>
  </div>
  <div class="field">
    <%= f.hidden_field :imageflavor %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
